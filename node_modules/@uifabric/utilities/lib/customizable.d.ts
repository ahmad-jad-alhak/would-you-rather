import * as React from 'react';
import * as PropTypes from 'prop-types';
export declare const CustomizableContextTypes: {
    customizations: PropTypes.Requireable<any>;
};
export declare function customizable(scope: string, fields: string[], concatStyles?: boolean): <P, S>(ComposedComponent: new (props: P, ...args: any[]) => React.Component<P, S>) => any;
